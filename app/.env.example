# Server
PORT=3007

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kalinova
DB_USER=kalinova_app
DB_PASSWORD=changeme
DB_SSL=false
DB_POOL_MAX=10

# Auth
# Generate a strong secret, e.g.: openssl rand -base64 48
JWT_SECRET=changeme

# CORS - comma-separated list of allowed origins, e.g.:
# ALLOWED_ORIGINS=https://kalinova.example.com,https://www.kalinova.example.com
ALLOWED_ORIGINS=
